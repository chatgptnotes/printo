# Base URL of the Python FastAPI backend (the VPS). Server-side only — never exposed
# to the browser. Locally point at the dev backend; in Vercel set the VPS URL (prefer https).
PRINTO_API_URL=http://127.0.0.1:8000

# Name of the HttpOnly session cookie that stores the backend JWT.
SESSION_COOKIE=printo_session
