fastapi==0.111.0
uvicorn==0.29.0
anthropic==0.28.0
streamlit==1.35.0
python-multipart==0.0.9
pydantic==2.7.0
python-dotenv==1.0.1
aiofiles==23.2.1
Pillow==10.3.0
requests==2.32.3
openpyxl==3.1.2
pypdf

# ── Phase 2: feature-extraction pipeline ──────────────────────────────
pdfplumber==0.11.4          # PDF text-layer prepass (deterministic, free)
pymupdf>=1.27               # render PDF pages to high-DPI images (fitz); 1.24.x had broken win wheels
pytesseract==0.3.13         # OCR fallback for scanned PDFs / images (needs Tesseract binary)
opencv-python-headless==4.10.0.84  # image cleanup: deskew, contrast, denoise
xhtml2pdf==0.2.16           # pure-Python HTML→PDF for the improvement doc
